{
  "name": "robotforge-next",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:clean": "powershell -NoProfile -Command \"if (Test-Path .next) { Remove-Item .next -Recurse -Force }; next dev\"",
    "build": "next build",
    "rebuild": "powershell -NoProfile -Command \"if (Test-Path .next) { Remove-Item .next -Recurse -Force }; next build\"",
    "start": "next start",
    "lint": "next lint",
    "generate-icons": "node scripts/generate-icons.mjs"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "@react-three/drei": "^10.7.7",
    "@react-three/fiber": "^9.5.0",
    "@ricky0123/vad-web": "^0.0.30",
    "@upstash/redis": "^1.36.3",
    "face-api.js": "^0.22.2",
    "next": "^15.5.0",
    "qrcode": "^1.5.4",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "three": "^0.183.2"
  }
}
